---
name: migration
description: "Shortcut for /maister-migration. Full migration workflow with rollback planning."
user-invocable: true
---

**User input**: `$ARGUMENTS`

Invoke `/maister-migration` with the above user input. Pass `$ARGUMENTS` verbatim.
