---
name: maister-quick-requirements-critic
description: Critique requirements quality with interactive 4-check rubric
---

**User input**: `$ARGUMENTS`

**ACTION REQUIRED**: This command delegates to a skill. Invoke the `maister-requirements-critic` skill via the `/maister-*` slash skill NOW with the user's command arguments. Do not execute the critique yourself.

Invoke `/maister-*` slash skill:
  skill: "maister-requirements-critic"
  args: "[user arguments from command]"
