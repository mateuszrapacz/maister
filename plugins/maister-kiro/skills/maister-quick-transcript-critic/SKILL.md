---
name: maister-quick-transcript-critic
description: Audit meeting transcripts for decision-process problems with structured critique report
---

**User input**: `$ARGUMENTS`

**ACTION REQUIRED**: This command delegates to a skill. Invoke the `maister-transcript-critic` skill via the `/maister-*` slash skill NOW with the user's command arguments. Do not execute the critique yourself.

Invoke `/maister-*` slash skill:
  skill: "maister-transcript-critic"
  args: "[user arguments from command]"
