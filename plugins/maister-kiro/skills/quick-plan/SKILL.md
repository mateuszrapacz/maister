---
name: quick-plan
description: "Shortcut for /maister-quick-plan. Lightweight plan under .maister/plans/ — not Kiro built-in /plan."
user-invocable: true
---

**User input**: `$ARGUMENTS`

Invoke `/maister-quick-plan` with the above user input. Pass `$ARGUMENTS` verbatim.
Do not use Kiro's built-in `/plan` — that switches to the Kiro Plan agent.
