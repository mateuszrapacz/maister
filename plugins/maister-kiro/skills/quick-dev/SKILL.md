---
name: quick-dev
description: "Shortcut for /maister-quick-dev. Implement directly with standards awareness — no full workflow."
user-invocable: true
---

**User input**: `$ARGUMENTS`

Invoke `/maister-quick-dev` with the above user input. Pass `$ARGUMENTS` verbatim.

