# @thermos

Invoke `/maister-thermos` for a combined thermo-nuclear branch review (security/correctness + code quality in parallel).

Gather the scoped diff and changed-file contents first, then run both review subagents and synthesize deduplicated findings.
