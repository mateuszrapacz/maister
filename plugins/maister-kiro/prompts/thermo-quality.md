# @thermo-quality

Invoke `/maister-thermo-nuclear-code-quality-review` for a strict maintainability audit of the current branch diff.

Gather diff and changed-file contents first. Apply the full thermo-nuclear code quality rubric.
