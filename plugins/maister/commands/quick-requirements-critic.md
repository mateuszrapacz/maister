---
name: maister-quick-requirements-critic
description: Critique requirements quality with interactive 4-check rubric
---

**ACTION REQUIRED**: This command delegates to a skill. Invoke the `requirements-critic` skill via the Skill tool NOW with the user's command arguments. Do not execute the critique yourself.

Invoke Skill tool:
  skill: "requirements-critic"
  args: "[user arguments from command]"
