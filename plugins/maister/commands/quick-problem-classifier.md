---
name: maister-quick-problem-classifier
description: Classify business requirements into modeling problem classes with targeted clarifying questions
---

**ACTION REQUIRED**: This command delegates to a skill. Invoke the `problem-classifier` skill via the Skill tool NOW with the user's command arguments. Do not execute the classification yourself.

Invoke Skill tool:
  skill: "problem-classifier"
  args: "[user arguments from command]"
