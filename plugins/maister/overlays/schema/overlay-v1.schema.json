{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://maister.local/schemas/overlay-v1.schema.json",
  "title": "Maister host overlay v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "overlay_id",
    "overlay_version",
    "target",
    "layout",
    "settings",
    "semantic_bindings",
    "inventory",
    "validation",
    "capabilities",
    "native_assets",
    "agent_projection"
  ],
  "properties": {
    "schema_version": { "const": 1 },
    "overlay_id": { "type": "string", "pattern": "^maister/(codex|cursor|kiro-cli|pi)$" },
    "overlay_version": { "$ref": "#/$defs/semver" },
    "compatibility": { "$ref": "#/$defs/compatibility" },
    "probes": { "$ref": "#/$defs/probes" },
    "target": { "$ref": "#/$defs/target" },
    "layout": {
      "type": "array",
      "items": { "$ref": "#/$defs/layoutEntry" },
      "minItems": 1
    },
    "settings": {
      "type": "array",
      "items": { "$ref": "#/$defs/setting" },
      "minItems": 1
    },
    "semantic_bindings": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "user_gate",
        "delegate_agent",
        "track_progress",
        "resolve_task_root",
        "persist_state",
        "continue_workflow"
      ],
      "properties": {
        "user_gate": { "$ref": "#/$defs/binding" },
        "delegate_agent": { "$ref": "#/$defs/binding" },
        "track_progress": { "$ref": "#/$defs/binding" },
        "resolve_task_root": { "$ref": "#/$defs/binding" },
        "persist_state": { "$ref": "#/$defs/binding" },
        "continue_workflow": { "$ref": "#/$defs/binding" }
      }
    },
    "inventory": { "$ref": "#/$defs/inventory" },
    "validation": { "$ref": "#/$defs/validation" },
    "capabilities": {
      "type": "object",
      "minProperties": 1,
      "patternProperties": {
        "^[a-z][a-z0-9_]*$": { "$ref": "#/$defs/capability" }
      },
      "additionalProperties": false
    },
    "native_assets": {
      "type": "array",
      "items": { "$ref": "#/$defs/nativeAsset" }
    },
    "agent_projection": { "$ref": "#/$defs/agentProjection" }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "target": {
            "properties": { "id": { "const": "pi" } }
          }
        }
      },
      "then": { "required": ["compatibility", "probes"] }
    }
  ],
  "$defs": {
    "semver": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
    },
    "relativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?!~)(?![A-Za-z]:[\\\\/])(?!.*\\\\)(?!.*(?:^|/)\\.\\.?(?:/|$)).+$"
    },
    "inventoryPath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?!~)(?![A-Za-z]:[\\\\/])(?!.*\\\\)(?!.*(?:^|/)\\.\\.?(?:/|$)).+$"
    },
    "mode": {
      "type": "string",
      "pattern": "^0[0-7]{3,4}$"
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "host_version_constraint", "discovery_roots"],
      "properties": {
        "id": { "enum": ["codex", "cursor", "kiro-cli", "pi"] },
        "host_version_constraint": { "type": "string", "minLength": 1 },
        "discovery_roots": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/relativePath" }
        },
        "adapter_id": { "type": "string", "minLength": 1 },
        "projection": { "type": "string", "minLength": 1 },
        "platform": { "enum": ["posix"] },
        "path_policy": { "$ref": "#/$defs/pathPolicy" }
      }
    },
    "layoutEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "destination", "kind", "mode", "ownership"],
      "properties": {
        "source": { "$ref": "#/$defs/relativePath" },
        "destination": { "$ref": "#/$defs/relativePath" },
        "kind": { "enum": ["file", "tree", "template"] },
        "mode": { "$ref": "#/$defs/mode" },
        "ownership": { "enum": ["whole_file", "managed_keys", "plugin_private"] }
      }
    },
    "setting": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "format", "ownership", "managed_keys", "merge_policy"],
      "properties": {
        "path": { "$ref": "#/$defs/relativePath" },
        "format": { "enum": ["json", "toml", "yaml", "shell"] },
        "ownership": { "enum": ["whole_file", "managed_keys", "managed_array_entries"] },
        "managed_keys": {
          "type": "array",
          "items": { "$ref": "#/$defs/relativePath" }
        },
        "array_path": { "$ref": "#/$defs/relativePath" },
        "identity": { "type": "string", "minLength": 1 },
        "entries": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["source"],
            "properties": { "source": { "type": "string", "minLength": 1 } }
          }
        },
        "merge_policy": { "const": "preserve_unmanaged_refuse_drift" }
      }
    },
    "binding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["adapter", "capability", "fail_closed"],
      "properties": {
        "adapter": { "type": "string", "minLength": 1 },
        "capability": { "type": "string", "minLength": 1 },
        "fail_closed": { "const": true }
      }
    },
    "inventory": {
      "type": "object",
      "additionalProperties": false,
      "required": ["required", "optional", "forbidden"],
      "properties": {
        "required": { "$ref": "#/$defs/pathList" },
        "optional": { "$ref": "#/$defs/pathList" },
        "forbidden": { "$ref": "#/$defs/pathList" },
        "command_origins": { "$ref": "#/$defs/originList" },
        "skill_origins": { "$ref": "#/$defs/originList" },
        "role_origins": { "$ref": "#/$defs/originList" },
        "support_inventory": { "type": "array" }
      }
    },
    "pathList": {
      "type": "array",
      "items": { "$ref": "#/$defs/inventoryPath" }
    },
    "validation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["forbidden_vocabulary", "executable_paths", "syntax_checks"],
      "properties": {
        "forbidden_vocabulary": { "$ref": "#/$defs/pathList" },
        "executable_paths": { "$ref": "#/$defs/pathList" },
        "syntax_checks": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[a-z]+:[^\\s].*$" }
        },
        "forbidden_topology": { "$ref": "#/$defs/pathList" }
      }
    },
    "originList": {
      "type": "array",
      "items": { "$ref": "#/$defs/origin" }
    },
    "origin": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "destination", "kind"],
      "properties": {
        "source": { "$ref": "#/$defs/relativePath" },
        "destination": { "$ref": "#/$defs/relativePath" },
        "kind": { "type": "string", "minLength": 1 },
        "transform_id": { "type": "string", "minLength": 1 },
        "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "logical_role_id": { "type": "string", "minLength": 1 }
      }
    },
    "pathPolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "agent_root_env",
        "default_agent_root",
        "settings_path",
        "session_root_env",
        "package_root_env",
        "package_path",
        "containment"
      ],
      "properties": {
        "agent_root_env": { "type": "string", "minLength": 1 },
        "default_agent_root": { "type": "string", "minLength": 1 },
        "settings_path": { "$ref": "#/$defs/relativePath" },
        "session_root_env": { "type": "string", "minLength": 1 },
        "package_root_env": { "type": "string", "minLength": 1 },
        "package_path": { "$ref": "#/$defs/relativePath" },
        "containment": { "const": "agent_root" }
      }
    },
    "compatibility": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pi", "node", "pi_subagents", "delegation_protocol"],
      "properties": {
        "pi": { "$ref": "#/$defs/semver" },
        "node": { "$ref": "#/$defs/semver" },
        "pi_subagents": { "$ref": "#/$defs/semver" },
        "delegation_protocol": { "const": 1 }
      }
    },
    "probes": {
      "type": "object",
      "additionalProperties": false,
      "required": ["executable", "prerequisite"],
      "properties": {
        "executable": {
          "type": "object",
          "additionalProperties": false,
          "required": ["command", "version_args", "expected_version", "resolve_realpath"],
          "properties": {
            "command": { "type": "string", "minLength": 1 },
            "version_args": { "type": "array", "items": { "type": "string", "minLength": 1 } },
            "expected_version": { "$ref": "#/$defs/semver" },
            "resolve_realpath": { "type": "boolean" }
          }
        },
        "prerequisite": {
          "type": "object",
          "additionalProperties": false,
          "required": ["package", "export", "version_range", "tested_version", "protocol_version"],
          "properties": {
            "package": { "type": "string", "minLength": 1 },
            "export": { "type": "string", "minLength": 1 },
            "version_range": { "type": "string", "minLength": 1 },
            "tested_version": { "$ref": "#/$defs/semver" },
            "protocol_version": { "const": 1 }
          }
        }
      }
    },
    "capability": {
      "type": "object",
      "additionalProperties": false,
      "required": ["class", "required_evidence"],
      "properties": {
        "class": { "enum": ["semantic", "safety", "persistence", "rollback", "packaging"] },
        "required_evidence": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "enum": ["E1", "E2", "E3", "E4", "E5", "E6"] }
        }
      }
    },
    "nativeAsset": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "destination", "mode", "sha256"],
      "properties": {
        "source": { "$ref": "#/$defs/relativePath" },
        "destination": { "$ref": "#/$defs/relativePath" },
        "mode": { "$ref": "#/$defs/mode" },
        "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    },
    "projectionDestination": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "path_template", "mode"],
      "properties": {
        "kind": { "type": "string", "minLength": 1 },
        "path_template": {
          "type": "string",
          "pattern": "^(?!/)(?!~)(?![A-Za-z]:[\\\\/])(?!.*\\\\)(?!.*(?:^|/)\\.\\.?(?:/|$))(?=[^{}]*(?:\\{role_id\\}[^{}]*)+$).+$"
        },
        "mode": { "$ref": "#/$defs/mode" }
      }
    },
    "supportAsset": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "source", "destination", "mode"],
      "properties": {
        "kind": { "type": "string", "minLength": 1 },
        "source": { "$ref": "#/$defs/relativePath" },
        "destination": { "$ref": "#/$defs/relativePath" },
        "mode": { "$ref": "#/$defs/mode" }
      }
    },
    "supportRole": {
      "type": "object",
      "additionalProperties": false,
      "required": ["support_id", "native_role_external_id", "assets"],
      "properties": {
        "support_id": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
        "native_role_external_id": { "type": "string", "minLength": 1 },
        "assets": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/supportAsset" }
        }
      }
    },
    "agentProjection": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "contract",
        "projector_version",
        "adapter_id",
        "representation",
        "canonical_roles",
        "destinations",
        "transform_ids",
        "execution_profile_ids",
        "support_inventory"
      ],
      "properties": {
        "schema_version": { "const": 1 },
        "contract": { "const": "agent-projection-v1.json" },
        "projector_version": { "$ref": "#/$defs/semver" },
        "adapter_id": { "enum": ["codex.exec", "cursor.native", "kiro-cli.native", "pi.native"] },
        "representation": { "enum": ["codex-prompt-schema", "cursor-markdown", "kiro-descriptor-prompt", "pi-agent-frontmatter"] },
        "canonical_roles": {
          "type": "array",
          "minItems": 28,
          "maxItems": 28,
          "uniqueItems": true,
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$" }
        },
        "destinations": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/projectionDestination" }
        },
        "transform_ids": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)*$" }
        },
        "execution_profile_ids": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)*$" }
        },
        "support_inventory": {
          "type": "array",
          "items": { "$ref": "#/$defs/supportRole" }
        }
      }
    }
  }
}
