# Implementation Plan: Upstream Merge Sync v2.2.1

## Overview

**Spec:** `implementation/spec.md`  
**Strategy:** `git merge upstream/master` → resolve 3 manifest conflicts → semantic review → `make build` → Kiro test fixes → `make validate` → work-log → **no commit**

**Risk:** Medium-high — 3 hard conflicts, 14 auto-merged orchestrator files, certain Kiro agent-count failure until updated.

**Source of truth:** `plugins/maister/` and `platforms/*/` only. Never hand-edit `plugins/maister-copilot/`, `maister-cursor/`, `maister-kiro/`, `maister-kilo/`.

**Fork version target:** `2.2.1-fork.1` on all 5 version-bearing manifests.

**Pre-merge reference SHAs (record in work-log):**
- Fork HEAD: `9f78d52` (`2.1.8-fork.2`)
- Upstream HEAD: `945f60b` (v2.2.1)
- Last sync point: `679958b` / marker `c790e51`

---

## Implementation Steps

### Task Group 1: Preparation

**Dependencies:** None  
**Files to Modify:** None (read-only git operations)

- [ ] 1.0 Complete preparation — safe merge baseline
  - [ ] 1.1 Confirm current branch is `master`
    - Run: `git branch --show-current`
    - **Acceptance:** Output is `master`
  - [ ] 1.2 Verify clean working tree
    - Run: `git status --porcelain`
    - **Acceptance:** Empty output (no staged/unstaged/untracked changes). If dirty, stash or commit unrelated work first — do not proceed with merge on dirty tree
  - [ ] 1.3 Create optional safety backup branch
    - Run: `git branch backup/pre-v2.2.1-merge`
    - **Acceptance:** `git branch --list backup/pre-v2.2.1-merge` shows branch; rollback point documented in work-log
  - [ ] 1.4 Fetch upstream remote
    - Run: `git fetch upstream`
    - **Acceptance:** `git rev-parse upstream/master` resolves to `945f60b` (or newer if upstream advanced — note actual SHA in work-log)
  - [ ] 1.5 Confirm expected divergence
    - Run: `git log --oneline 679958b..upstream/master`
    - **Acceptance:** Exactly **4 commits** (operator visibility `8d9e3b3`, version `68f3720`, init fix `56f7e7b`, version `945f60b`)
    - Run: `git rev-list --left-right --count HEAD...upstream/master`
    - **Acceptance:** Output shows **53 / 4** (fork-only / upstream-only) or document actual counts if drifted
  - [ ] 1.6 Record pre-merge state in work-log draft
    - Note fork HEAD SHA, upstream HEAD SHA, merge-base, divergence counts
    - **Acceptance:** SHAs captured for Group 7 finalization

**Group Acceptance Criteria:**
- On `master` with clean tree
- `upstream/master` fetched and reachable
- Divergence matches spec (4 upstream commits, ~53 fork-only)
- Backup branch exists (optional but recommended)

---

### Task Group 2: Git Merge + Conflict Resolution

**Dependencies:** Group 1 complete  
**Files to Modify:**
- `.claude-plugin/marketplace.json` (conflict)
- `plugins/maister/.claude-plugin/plugin.json` (conflict)
- `plugins/maister-copilot/.claude-plugin/plugin.json` (conflict)
- `.cursor-plugin/marketplace.json` (bump only)
- `plugins/maister-cursor/.cursor-plugin/plugin.json` (bump only)

- [ ] 2.0 Complete merge and version resolution
  - [ ] 2.1 Execute upstream merge
    - Run: `git merge upstream/master -m "Merge upstream v2.2.1 (operator visibility layer)"`
    - **Acceptance:** Merge completes (may pause for conflicts). If merge aborts unexpectedly, **stop** — do not force; consult rollback plan
  - [ ] 2.2 Resolve 3 manifest version conflicts → `2.2.1-fork.1`
    - Edit conflicted files; set `"version": "2.2.1-fork.1"` in version fields
    - Preserve fork marketplace naming (`maister-plugins` or fork-specific name — do not revert to upstream-only identity)
    - Take upstream JSON structure and non-conflicting description fields where applicable
    - Files:
      - `.claude-plugin/marketplace.json`
      - `plugins/maister/.claude-plugin/plugin.json`
      - `plugins/maister-copilot/.claude-plugin/plugin.json`
    - **Acceptance:** No `<<<<<<<` / `=======` / `>>>>>>>` markers remain in any file; all 3 manifests read `2.2.1-fork.1`
  - [ ] 2.3 Bump non-conflicting manifests to `2.2.1-fork.1`
    - `.cursor-plugin/marketplace.json`
    - `plugins/maister-cursor/.cursor-plugin/plugin.json`
    - **Acceptance:** Both files show `"version": "2.2.1-fork.1"` (no conflict markers)
  - [ ] 2.4 Stage conflict resolutions without committing
    - Run: `git add` on resolved manifest files
    - **Acceptance:** `git status` shows merge in progress or staged resolutions; **no commit created** (per FR-11)
  - [ ] 2.5 Verify manifest uniformity (5/5)
    - Run: `grep -r '"version"' .claude-plugin/marketplace.json plugins/maister/.claude-plugin/plugin.json plugins/maister-copilot/.claude-plugin/plugin.json .cursor-plugin/marketplace.json plugins/maister-cursor/.cursor-plugin/plugin.json`
    - **Acceptance:** All 5 files contain `"version": "2.2.1-fork.1"`
  - [ ] 2.6 Scan for stray conflict markers repo-wide
    - Run: `rg '^(<<<<<<<|=======|>>>>>>>)' --glob '!*.orig'`
    - **Acceptance:** Zero matches (or only in files that will be regenerated by `make build` — if so, note in work-log and proceed to Group 4)

**Group Acceptance Criteria:**
- Upstream v2.2.1 content integrated via merge (not cherry-pick-only)
- 3 hard conflicts resolved with fork version `2.2.1-fork.1`
- 5/5 manifests at `2.2.1-fork.1`
- Changes staged/unstaged but **not committed**

---

### Task Group 3: Semantic Review

**Dependencies:** Group 2 complete  
**Files to Review/Modify (source only — `plugins/maister/`):**
- `plugins/maister/skills/orchestrator-framework/references/orchestrator-patterns.md`
- `plugins/maister/skills/development/SKILL.md`
- `plugins/maister/skills/product-design/SKILL.md`
- `plugins/maister/skills/init/SKILL.md`
- `plugins/maister/CLAUDE.md`
- `plugins/maister/skills/migration/SKILL.md` (diff review)
- `plugins/maister/skills/performance/SKILL.md` (diff review)
- `plugins/maister/skills/research/SKILL.md` (diff review)

- [ ] 3.0 Complete semantic review of auto-merged orchestrator files
  - [ ] 3.1 Review `orchestrator-patterns.md`
    - Confirm §7 (TL;DR artifact contract), §8 (dashboard integration), §9 (HTML companions) present
    - Confirm fork-specific references intact (CHAT GATE patterns, platform transform notes)
    - **Acceptance:** All three sections present; no fork content deleted
  - [ ] 3.2 Review `development/SKILL.md`
    - Operator visibility blocks present (dashboard, HTML companions, config.yml gate)
    - Timestamp rule (`date -u` capture at init) present
    - Fork CHAT GATE / delegation patterns not regressed
    - **Acceptance:** Upstream visibility layer + fork gate patterns coexist
  - [ ] 3.3 Review `product-design/SKILL.md`
    - HTML companion / inline brief flow present
    - Visibility integration (dashboard, TL;DR) intact
    - Fork gates and AJ soft-suggestion blocks preserved
    - **Acceptance:** Product-design orchestrator supports HTML companions without fork regression
  - [ ] 3.4 Review `init/SKILL.md`
    - Copilot `argument-hint` array quoting fix from commit `56f7e7b` present
    - `.maister/config.yml` scaffolding with `html_output` gate (default `true`) present
    - **Acceptance:** Init skill includes upstream fix and config scaffolding
  - [ ] 3.5 Review `plugins/maister/CLAUDE.md`
    - All **12 AJ skills** documented: aggregate-designer, context-distiller, grill-me, linguistic-boundary-verifier, metaprogram-classifier, problem-classifier, requirements-critic, test-strategy-reviewer, thermo-nuclear-code-quality-review, thermo-nuclear-review, thermos, transcript-critic
    - Bundles A–D chain documentation intact
    - grill-me, thermos, thermo-nuclear entries present
    - Upstream visibility docs merged correctly
    - **Acceptance:** 12/12 fork skills + thermo entries present; upstream visibility sections added
  - [ ] 3.6 Diff review — migration, performance, research orchestrators
    - Run: `git diff 679958b..HEAD -- plugins/maister/skills/{migration,performance,research}/SKILL.md` (or equivalent post-merge diff)
    - Check gate/dashboard additions do not remove fork-specific content
    - **Acceptance:** No fork regression; upstream visibility additions present
  - [ ] 3.7 Verify upstream-only assets present (clean adds)
    - Confirm files exist:
      - `plugins/maister/skills/orchestrator-framework/assets/dashboard.html`
      - `plugins/maister/skills/orchestrator-framework/references/html-report-style.md`
      - `plugins/maister/agents/html-companion-writer.md`
    - **Acceptance:** 3/3 files present in source tree
  - [ ] 3.8 Fix semantic corruption in source only (if needed)
    - Edit only `plugins/maister/` — never generated variants
    - **Acceptance:** Any fixes applied in source; corruption items documented in work-log (or N/A if clean)

**Group Acceptance Criteria:**
- Operator visibility layer (§7–9, config.yml gate, TL;DR) present in source
- Fork features preserved in orchestrators and CLAUDE.md (12 AJ skills, thermo-nuclear, bundles)
- 3 upstream-only assets confirmed present
- No direct edits to generated platform variants

---

### Task Group 4: Platform Rebuild

**Dependencies:** Group 3 complete (source stable)  
**Commands:** `make build`

- [ ] 4.0 Regenerate all four platform variants from source
  - [ ] 4.1 Run full platform build
    - Run: `make build`
    - **Acceptance:** Exit code 0; Copilot, Cursor, Kiro, and Kilo variants regenerated without error
  - [ ] 4.2 Verify key upstream assets propagated to generated output
    - Cursor: `plugins/maister-cursor/agents/` contains html-companion-writer (prefixed name, e.g. `maister-html-companion-writer` or equivalent)
    - Kiro: `plugins/maister-kiro/agents/maister-html-companion-writer.json` exists
    - Dashboard asset copied where applicable
    - Run: `rg -l 'html-companion-writer|dashboard\.html' plugins/maister-{copilot,cursor,kiro,kilo}/`
    - **Acceptance:** References found in all four generated variants
  - [ ] 4.3 Spot-check Cursor override wiring (OQ-3)
    - Confirm `platforms/cursor/build.sh` step 12 still applies `quick-dev` / `quick-plan` overrides
    - **Acceptance:** Override step intact; no corruption from merge

**Group Acceptance Criteria:**
- `make build` completes successfully
- html-companion-writer and dashboard.html present in generated variants
- Cursor overrides still wired in build script

---

### Task Group 5: Kiro Fixes

**Dependencies:** Group 4 complete (initial build done)  
**Files to Modify:**
- `platforms/kiro-cli/tests/e2e-matrix.test.sh`
- `platforms/kiro-cli/tests/build-completion.test.sh`
- `platforms/kiro-cli/build.sh` (comment ~L780)
- `platforms/kiro-cli/tests/validation.test.sh` (rule 26 thresholds — if needed)
- `Makefile` (`validate-kiro` rule 26 thresholds — if needed)
- `platforms/kiro-cli/build.sh` (trustedAgents — only if validation fails, OQ-2)

- [ ] 5.0 Complete Kiro fork-specific test and threshold fixes
  - [ ] 5.1 Update agent count assertions **26 → 27**
    - `platforms/kiro-cli/tests/e2e-matrix.test.sh` (L79–98): `test ... -eq 26` → `-eq 27`; update assert message
    - `platforms/kiro-cli/tests/build-completion.test.sh` (L46–83): `test "$count" -eq 26` → `-eq 27`; update assert message
    - `platforms/kiro-cli/build.sh` (~L780): comment `26 subagents` → `27 subagents`
    - **Acceptance:** No remaining hardcoded `26` agent-count assertions in these three files (except rule-number references like "rule 26")
  - [ ] 5.2 Rebuild Kiro variant and measure CHAT GATE counts
    - Run: `make build-kiro`
    - Measure:
      - `dev_count=$(grep -c 'CHAT GATE' plugins/maister-kiro/skills/maister-development/SKILL.md || true)`
      - `total=$(grep -r 'CHAT GATE' plugins/maister-kiro/skills/ --include='*.md' | wc -l | tr -d ' ')`
    - Record measured values in work-log
    - **Acceptance:** Build succeeds; measured `dev_count` and `total` documented
  - [ ] 5.3 Adjust CHAT GATE rule 26 thresholds if counts exceed current minimums
    - Current thresholds: `dev_count >= 53`, `total >= 200`
    - If measured values are below thresholds → investigate transform regression (do not lower without cause)
    - If measured values exceed thresholds but tests fail → update to measured values (use floor of measured count)
    - Files to update if needed:
      - `platforms/kiro-cli/tests/validation.test.sh` (`test_chat_gate_count_threshold`)
      - `Makefile` (`validate-kiro` lines ~148–150)
    - **Acceptance:** Thresholds match post-build reality; old and new values documented in work-log
  - [ ] 5.4 Verify html-companion-writer Kiro wiring (OQ-2)
    - If Kiro smoke/validation fails on agent delegation, add `trustedAgents` entry in `platforms/kiro-cli/build.sh`
    - **Acceptance:** Agent delegation passes or explicit N/A documented with passing tests
  - [ ] 5.5 Re-run full build if platform scripts were edited
    - Run: `make build` (if any edits in 5.1–5.4)
    - **Acceptance:** All four variants regenerated after Kiro script changes

**Group Acceptance Criteria:**
- All hardcoded 26-agent assertions updated to 27
- CHAT GATE rule 26 thresholds empirically aligned (validation.test.sh + Makefile)
- Kiro build output contains exactly 27 agent JSON files
- Full rebuild completed after any platform script edits

---

### Task Group 6: Validation

**Dependencies:** Group 5 complete  
**Commands:** `make validate`, Kiro extended test suite

- [ ] 6.0 Complete validation gate — all platforms green
  - [ ] 6.1 Run full validation suite
    - Run: `make validate`
    - **Acceptance:** Exit code 0; Copilot, Cursor, Kiro, and Kilo validators PASS
  - [ ] 6.2 Run per-platform validators (if isolating failures)
    - `make validate-copilot` — includes init `argument-hint` structural checks
    - `make validate-cursor` — includes quick-plan integrity guard
    - `make validate-kiro` — includes rule 26 CHAT GATE threshold
    - `make validate-kilo`
    - **Acceptance:** Each target PASS individually
  - [ ] 6.3 Run Kiro extended test suite
    - `platforms/kiro-cli/tests/build-core.test.sh`
    - `platforms/kiro-cli/tests/build-completion.test.sh` (27 agents)
    - `platforms/kiro-cli/tests/e2e-matrix.test.sh` (27 agents)
    - `platforms/kiro-cli/tests/chat-gate.test.sh`
    - `platforms/kiro-cli/tests/validation.test.sh` (rule 26)
    - **Acceptance:** All 5 scripts exit 0
  - [ ] 6.4 Optional Kilo smoke test
    - Run: `platforms/kilo-cli/smoke-cli.sh` (if available)
    - **Acceptance:** PASS or documented skip reason
  - [ ] 6.5 Final grep verification of generated variants
  - Confirm `html-companion-writer` and `dashboard.html` in built output
    - Run: `find plugins/maister-kiro/agents -name '*.json' | wc -l` → **27**
    - **Acceptance:** Agent count = 27; upstream assets grep-positive in all variants

**Group Acceptance Criteria:**
- `make validate` PASS (all 4 platforms)
- Kiro extended test suite PASS (5 scripts)
- 27 Kiro agent JSON files in built output
- On validation failure: **stop**, analyze root cause, attempt targeted fix (Group 5 or Group 3), re-run — do not auto-rollback without user confirmation

---

### Task Group 7: Documentation

**Dependencies:** Group 6 complete (or document partial state with failures)  
**Files to Create/Modify:**
- `implementation/work-log.md`

- [ ] 7.0 Complete handoff documentation — no commit
  - [ ] 7.1 Create/update `implementation/work-log.md`
    - Pre-merge SHAs (fork HEAD, upstream HEAD, merge-base)
    - Merge strategy and fork version decision (`2.2.1-fork.1`)
    - Files touched (manifests, Kiro tests, semantic fixes if any)
    - CHAT GATE threshold old → new values (if changed)
    - Test results summary (`make validate`, Kiro extended tests)
    - Known limitations (Kiro Terminal browser open, E2E dashboard out of scope)
    - Open questions resolved (OQ-1 through OQ-4)
    - **Acceptance:** Work-log complete per FR-10; readable handoff for user review
  - [ ] 7.2 Confirm no git commit created
    - Run: `git log -1 --oneline` — should still show pre-merge fork HEAD or merge commit only if user later commits
    - Run: `git status` — shows uncommitted merge resolution, version bumps, rebuild output, test fixes
    - **Acceptance:** No new commit by implementation agent (per FR-11 / user decision)
  - [ ] 7.3 Final manifest version check
    - Run: `grep '"version"' .claude-plugin/marketplace.json plugins/maister/.claude-plugin/plugin.json plugins/maister-copilot/.claude-plugin/plugin.json .cursor-plugin/marketplace.json plugins/maister-cursor/.cursor-plugin/plugin.json`
    - **Acceptance:** All read `2.2.1-fork.1`
  - [ ] 7.4 Summarize handoff state for user
    - List: merge complete, versions bumped, build green, tests green, ready for user commit review
    - Note: push to remote out of scope unless user requests
    - **Acceptance:** User can review `git diff` and work-log before deciding commit structure

**Group Acceptance Criteria:**
- `implementation/work-log.md` complete with SHAs, decisions, test output
- Working tree contains all changes uncommitted
- 5/5 manifests at `2.2.1-fork.1`
- Handoff ready for user commit decision

---

## Rollback Reference

| Phase | Action | Requires user confirmation |
|-------|--------|---------------------------|
| During merge | `git merge --abort` | No |
| After merge, before commit | `git reset --hard backup/pre-v2.2.1-merge` | Yes |
| No backup | `git reflog` → reset to `9f78d52` | Yes |
| Validation failure | Targeted fix → re-validate; rollback only if user prefers | Yes for reset |

---

## Success Metrics (from spec)

| Metric | Target |
|--------|--------|
| Upstream commits integrated | 4 |
| Hard merge conflicts resolved | 3 |
| Manifest version uniformity | 5/5 at `2.2.1-fork.1` |
| Kiro agent JSON count | 27 |
| `make validate` | PASS |
| Kiro extended tests | PASS (5 scripts) |
| Fork-only skills in CLAUDE.md | 12/12 |
| New upstream assets in source | 3/3 |
| Git commit created | **None** |

---

## Plan Summary

| | Count |
|---|------|
| **Task groups** | **7** |
| **Total steps** | **37** (plus 7 group-completion milestones) |

**Step breakdown by group:**

| Group | Steps |
|-------|-------|
| 1 — Preparation | 6 |
| 2 — Git merge + conflict resolution | 6 |
| 3 — Semantic review | 8 |
| 4 — Platform rebuild | 3 |
| 5 — Kiro fixes | 5 |
| 6 — Validation | 5 |
| 7 — Documentation | 4 |
