---
name: maister-quick-dev
description: Implement a task directly with Maister standards enforcement (no planning mode)
---

**ACTION REQUIRED**: This command delegates to a skill. Invoke the `quick-dev` skill via the Skill tool NOW with the user's command arguments. Do not execute the workflow yourself.

Invoke Skill tool:
  skill: "quick-dev"
  args: "[user arguments from command]"
