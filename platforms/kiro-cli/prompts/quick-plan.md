# @quick-plan

Invoke `/maister-quick-plan` with the user's task or feature description.

Produces a lightweight plan under `.maister/plans/` without full development workflow.

Do not use Kiro's built-in `/plan` — that switches to the Kiro Plan agent, not this workflow.
